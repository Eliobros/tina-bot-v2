
// commands/infobot.js
module.exports = {
  name: "infobot",
  description: "Mostra informações sobre o bot",
  async execute(sock, { from, pushName }) {
    // Sua lógica existente do comando infobot aqui
    // Todo o código que estava dentro do if(text.startsWith("/infobot")) ...
  }
};